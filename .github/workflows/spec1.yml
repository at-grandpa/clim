name: clim CI

on: [push]

jobs:
  spec:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: make spec/1 NUM_OF_JOBS=5
      run: make spec/1 NUM_OF_JOBS=5
